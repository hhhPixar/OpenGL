/*
 * glpch.cpp (预编译头占位实现)
 * ----------------------------------------------------------------------------
 * 这个文件是什么:
 *     预编译头对应的 cpp,内容只有一行 #include "glpch.h"。
 *
 * 在引擎里的角色:
 *     预编译头机制需要"有翻译单元去编译 glpch.h"才能生成 .pch / .gch 缓存文件。
 *     本 cpp 就是那个被编译来产出缓存的翻译单元——它本身不需要任何逻辑,
 *     只是让编译器把 glpch.h 好好编译一遍、存下结果。
 *
 * 关键依赖:
 *     - glpch.h : 全工程共用的预编译头(预解析后供其它 cpp 复用)
 */

#include "glpch.h"
